# Numina Backend Environment Variables

# Database Configuration
DATABASE_TYPE=h2  # or postgresql
# DATABASE_JDBC_URL=jdbc:postgresql://localhost:5432/numina
# DATABASE_USER=postgres
# DATABASE_PASSWORD=postgres

# Firebase Cloud Messaging
# Download your Firebase service account JSON file from:
# Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Place the file in src/main/resources/ and set the path below
# IMPORTANT: Add firebase-service-account.json to .gitignore!
FIREBASE_SERVICE_ACCOUNT_PATH=src/main/resources/firebase-service-account.json

# JWT Configuration
JWT_SECRET=your-secret-key-here-change-in-production
JWT_ISSUER=numina-backend
JWT_AUDIENCE=numina-users
JWT_REALM=numina

# Server Configuration
SERVER_PORT=8080
ENVIRONMENT=development
