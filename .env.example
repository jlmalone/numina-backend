# Numina Backend Environment Variables

# Database Configuration
DATABASE_URL=jdbc:postgresql://localhost:5432/numina
DB_USER=numina
DB_PASSWORD=your_password_here

# JWT Configuration
JWT_SECRET=your_jwt_secret_here
JWT_ISSUER=numina-backend
JWT_AUDIENCE=numina-clients
JWT_REALM=numina

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379

# Firebase Cloud Messaging
# Download your Firebase service account JSON file from:
# Firebase Console > Project Settings > Service Accounts > Generate New Private Key
# Place the file in config/ directory and set the path below
# IMPORTANT: Add firebase-credentials.json to .gitignore!
FCM_CREDENTIALS_PATH=/path/to/firebase-credentials.json

# CORS Configuration
ALLOWED_HOSTS=http://localhost:3000,https://numina.app

# Environment
ENVIRONMENT=production
